- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:

    # git
    ~/.gitignore: gitignore
    ~/.gitconfig: gitconfig

    # vim / neovim
    ~/.vim: vim
    ~/.vimrc: vimrc

    # unix
    ~/.zshrc: zshrc

    # macos
    ~/.Brewfile: homebrew/Brewfile
